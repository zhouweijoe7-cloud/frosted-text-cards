---
name: frosted-text-cards
description: 将中文长文本精准提炼为一组关键词文字卡片，并在生成前提供配色与版式选择，最终制作半透明磨砂质感的透明 PNG。用于知识分享、教程、产品功能、经验总结和社交媒体图文卡片；不用于普通照片修图或纯插画生成。
---

# Frosted Text Cards

把用户提供的原始文本转化为文案准确、系列统一、可直接发布的 4:5 磨砂文字卡片。

## 工作流

1. 保留产品名、术语、数字、因果关系和原文立场；清理口语重复、转义字符与无意义停顿。
2. 按“一张卡只表达一个重点”拆分文本。卡片数量由有效观点决定，通常为 4–8 张。
3. 按 [卡片数据结构](references/card-schema.md) 输出文案规划。
4. 生成图片前设置两个确认节点：
   - 用户尚未选择色调时，先给出一组配色方案，其中包含彩色、黑白灰和暗调方案。
   - 用户尚未选择版式时，展示 [版式规则](references/layout-rules.md) 中的可用版式并等待选择。
5. 用户确认后，为每张卡自动匹配最合适的版式。保持系列内字体、磨砂材质、边缘高光、元数据体系与安全边距一致。
6. 采用抽象字景、色块、折射纹理或几何结构作为视觉底层。只有用户明确提供并要求使用照片时，才引入照片。
7. 确认配色与文案后，读取 [渲染规范](references/rendering.md)，调用确定性渲染脚本生成图片。
8. 输出透明 PNG 时，保留真实 RGBA 透明通道。文字必须通过确定性排版生成，避免把中文交给图像模型直接绘制。
9. 交付前检查文字准确性、换行、边距、对比度、透明通道、尺寸一致性和系列连贯性。

## 已确认的视觉基线

- 默认配色：P12 蓝黑钢。
- 默认版式：L01 裁切字景。
- 可用版式：L01、L03、L04、L07、L08、L09、L10。
- 默认画幅：1080 × 1350 px，4:5 竖版。
- 默认材质：冷灰半透明玻璃、柔化背景字形、细边缘高光、轻微颗粒与克制阴影。
- 字体组合与字号层级见 [字体系统](references/typography.md)。
- 图片内容缺省时，使用抽象视觉结构；不主动生成具象照片底图。
- 十二套配色的准确颜色、适用内容和自动推荐规则见 [配色令牌](references/palettes.md)。

## 输出顺序

先提供卡片文案表，再提供配色选择，再提供版式选择。用户确认后生成 PNG，并附系列预览和单张文件。
